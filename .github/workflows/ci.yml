name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  wasm-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build pglite WASM
        working-directory: wasm-build
        run: DEBUG=false make build

      - name: Unpack WASM distribution
        working-directory: wasm-build
        run: make unpack

      - name: Upload WASM artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pglite-wasm-dist
          retention-days: 1
          path: |
            wasm-build/output/pglite.wasi
            core/src/main/resources/pgdata/
            core/src/main/resources/pglite-files.txt
            core/src/main/resources/pglite-dirs.txt

  build:
    needs: wasm-build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-26-intel, windows-latest ]
        java: [ 17, 21, 25 ]

    steps:
      - uses: actions/checkout@v4

      - name: Download WASM artifacts
        uses: actions/download-artifact@v4
        with:
          name: pglite-wasm-dist

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: maven

      - name: Build and test
        run: mvn -B install
        env:
          MAVEN_OPTS: -Xmx2g

      - name: Publish Test Report
        if: always()
        uses: mikepenz/action-junit-report@v5
        with:
          report_paths: '**/target/surefire-reports/TEST-*.xml'
